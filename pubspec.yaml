name: sqlite_async
description: High-performance asynchronous interface for SQLite on Dart and Flutter.
version: 0.7.0-alpha.3
repository: https://github.com/powersync-ja/sqlite_async.dart
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  drift: ^2.15.0
  # In order to compile custom web worker
  # drift:
  #   git:
  #     url: https://github.com/powersync-ja/drift.git
  #     ref: test # branch name
  #     path: drift
  sqlite3: '^2.3.0'
  js: ^0.6.3
  async: ^2.10.0
  collection: ^1.17.0
  mutex: ^3.1.0
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0
  test_api: ^0.7.0
  glob: ^2.1.1
  benchmarking: ^0.6.1
  shelf: ^1.4.1
  shelf_static: ^1.1.2
  stream_channel: ^2.1.2
  path: ^1.9.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
  web:
